# DL3018: "Pin versions in apk add". Impractical for Alpine `--no-cache` builds —
# the package index rotates, so pinned apk versions break the build when a
# mirror drops the exact version. We accept unpinned apk packages here.
ignored:
  - DL3018
