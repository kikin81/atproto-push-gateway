// Loaded by the commitlint pre-commit hook. The ruleset itself lives in
// @open-turo/commitlint-config-conventional (declared as the hook's
// additional_dependency in .pre-commit-config.yaml). Mirrors nubecita.
module.exports = { extends: ["@open-turo/commitlint-config-conventional"] };
